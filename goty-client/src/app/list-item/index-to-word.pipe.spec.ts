import { IndexToWordPipe } from './index-to-word.pipe';

describe('IndexToWordPipe', () => {
  it('create an instance', () => {
    const pipe = new IndexToWordPipe();
    expect(pipe).toBeTruthy();
  });
});
